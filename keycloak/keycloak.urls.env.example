# Keycloak dev URL configuration (NOT committed when copied to keycloak.urls.env)
# Origin is the scheme+host+port of the PWA.
KC_PWA_ORIGIN=http://localhost
# Redirect base is the full base path used by the PWA in dev; '*' will be appended.
KC_PWA_REDIRECT_BASE=http://localhost/pwa-modeller/
